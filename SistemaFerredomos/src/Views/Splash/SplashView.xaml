<Window x:Class="SistemaFerredomos.src.Views.Splash.SplashView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Ferredomos"
        Height="320" Width="480"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#1D9E75">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Contenido principal -->
        <StackPanel Grid.Row="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <!-- Logo en círculo -->
            <Border Width="110" Height="110"
                    Background="White"
                    CornerRadius="55"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,20">
                <Image Source="/src/Assets/logo.png"
                       Stretch="Uniform"
                       Margin="10"/>
            </Border>

            <!-- Nombre -->
            <TextBlock Text="Ferredomos"
                       Foreground="White"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="de Durango"
                       Foreground="#A8E6D0"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>

            <!-- Mensaje de estado -->
            <TextBlock x:Name="StatusText"
                       Text="Iniciando sistema..."
                       Foreground="#D0F0E6"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,0"/>
        </StackPanel>

        <!-- Barra de progreso -->
        <StackPanel Grid.Row="1" Margin="40,0,40,30">
            <ProgressBar x:Name="ProgressBar"
                         Height="6"
                         Minimum="0" Maximum="100"
                         Value="0"
                         Background="#0F6E56"
                         Foreground="White"
                         BorderThickness="0"/>
            <TextBlock x:Name="PercentText"
                       Text="0%"
                       Foreground="#A8E6D0"
                       FontSize="11"
                       HorizontalAlignment="Right"
                       Margin="0,4,0,0"/>
        </StackPanel>
    </Grid>
</Window>
