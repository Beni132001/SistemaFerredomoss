<Window x:Class="SistemaFerredomos.src.Views.Login.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaFerredomos.src.Views.Login"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Usuario:"/>
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Grid.Row="2" Text="Contraseña:"/>
        <PasswordBox Grid.Row="3" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,5,0,20"/>

        <Button Grid.Row="4" 
                Content="Iniciar Sesión" 
                Command="{Binding LoginCommand}"
                Padding="10,5" 
                HorizontalAlignment="Center" Margin="0,176,0,49" Width="80"/>

        <TextBlock Grid.Row="5" 
                   Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   HorizontalAlignment="Center" 
                   Margin="0,10,0,0"/>
    </Grid>
</Window>
