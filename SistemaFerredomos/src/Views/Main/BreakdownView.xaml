<UserControl x:Class="SistemaFerredomos.src.Views.Main.BreakdownView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F4F6F8">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- 🔵 PANEL IZQUIERDO -->
        <Border Grid.Column="0" Margin="10"
                Background="White"
                CornerRadius="10"
                Padding="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Gestión de Desgloses"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#2C3E50"/>
                </StackPanel>

                <!-- BOTONES -->
                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">

                    <Button Content="➕ Nuevo"
                            Command="{Binding AddCommand}"
                            Background="#1D9E75"
                            Foreground="White"
                            Padding="10,5"
                            Margin="5"
                            BorderThickness="0"/>

                    <Button Content="🗑 Eliminar"
                            Command="{Binding DeleteCommand}"
                            Background="#E74C3C"
                            Foreground="White"
                            Padding="10,5"
                            Margin="5"
                            BorderThickness="0"/>

                    <Button Content="📥 Excel"
                            Command="{Binding ExportCommand}"
                            Background="#3498DB"
                            Foreground="White"
                            Padding="10,5"
                            Margin="5"
                            BorderThickness="0"/>
                </StackPanel>

                <!-- BUSCADOR -->
                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">
                    <TextBox Width="200"
                             Margin="5"
                             VerticalContentAlignment="Center"
                             Text="{Binding SearchOrder}" />
                    <Button Content="Buscar"
                            Command="{Binding LoadCommand}"
                            Margin="5"/>
                </StackPanel>

                <!-- TABLA -->
                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding BreakdownList}"
                          SelectedItem="{Binding SelectedBreakdown}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          RowHeight="32"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2C3E50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Orden" Binding="{Binding OrderNumber}" Width="*"/>
                        <DataGridTextColumn Header="Perfil" Binding="{Binding ProfileName}" Width="*"/>
                        <DataGridTextColumn Header="Medida" Binding="{Binding Size}" Width="*"/>
                        <DataGridTextColumn Header="Color" Binding="{Binding Color}" Width="*"/>
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity}" Width="*"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding CreatedAt}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>

        <!-- FORMULARIO DERECHO -->
        <Border Grid.Column="1"
                Margin="10"
                Background="White"
                CornerRadius="10"
                Padding="15">

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsFormVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            
            <StackPanel>

                <TextBlock Text="Nuevo Desglose"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <!-- CAMPOS -->
                <StackPanel Margin="0,5">

                    <TextBlock Text="Número de Orden"/>
                    <TextBox Text="{Binding OrderNumber}" Margin="0,5"/>

                    <TextBlock Text="Perfil"/>
                    <ComboBox ItemsSource="{Binding ProfileList}"
                              SelectedItem="{Binding SelectedProfile}"
                              DisplayMemberPath="Name"
                              Margin="0,5"/>

                    <TextBlock Text="Medida"/>
                    <TextBox Text="{Binding Size}" Margin="0,5"/>

                    <TextBlock Text="Color"/>
                    <TextBox Text="{Binding Color}" Margin="0,5"/>

                    <TextBlock Text="Cantidad"/>
                    <TextBox Text="{Binding Quantity}" Margin="0,5"/>

                </StackPanel>

                <!-- BOTONES -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,10,0,0">

                    <Button Content="Guardar"
                            Background="#1D9E75"
                            Foreground="White"
                            Width="100"
                            Margin="5"
                            Command="{Binding SaveCommand}"/>

                    <Button Content="Cancelar"
                            Background="#BDC3C7"
                            Width="100"
                            Margin="5"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>