<UserControl x:Class="SistemaFerredomos.src.Views.Main.AddEditProductionView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">

        <StackPanel>

            <TextBlock Text="Diseño de Producción"
           FontSize="20"
           FontWeight="Bold"
           Margin="0,0,0,15"/>

            <TextBlock Text="Nombre"/>
            <TextBox Text="{Binding Production.Name}"/>

            <TextBlock Text="Tipo"/>
            <ComboBox SelectedItem="{Binding Production.Type}">
                <ComboBoxItem Content="vidrios"/>
                <ComboBoxItem Content="domos"/>
                <ComboBoxItem Content="ventanas"/>
                <ComboBoxItem Content="puertas"/>
            </ComboBox>

            <TextBlock Text="Precio"/>
            <TextBox Text="{Binding Production.Price}"/>

            <TextBlock Text="Altura"/>
            <TextBox Text="{Binding Production.Height}"/>

            <TextBlock Text="Ancho"/>
            <TextBox Text="{Binding Production.Width}"/>

            <TextBlock Text="Largo"/>
            <TextBox Text="{Binding Production.Length}"/>

            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">

                <Button Content="Guardar"
        Command="{Binding SaveCommand}"
        Width="100"/>

                <Button Content="Cancelar"
        Command="{Binding CancelCommand}"
        Margin="10,0,0,0"
        Width="100"/>

            </StackPanel>

        </StackPanel>

    </Grid>
</UserControl>