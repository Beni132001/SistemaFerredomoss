<UserControl x:Class="SistemaFerredomos.src.Views.Main.AddEditProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Nombre" Margin="0,5"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Stock" Margin="0,5"/>
            <TextBox Text="{Binding Stock, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Precio Compra" Margin="0,5"/>
            <TextBox Text="{Binding PurchasePrice, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Precio Venta" Margin="0,5"/>
            <TextBox Text="{Binding SalePrice, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Proveedor" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding Suppliers}" 
                      SelectedItem="{Binding SelectedSupplier}" 
                      DisplayMemberPath="Name" />

            <TextBlock Text="Imagen" Margin="0,5"/>
            <TextBox Text="{Binding Image, UpdateSourceTrigger=PropertyChanged}" />

            <Button Content="Guardar" Command="{Binding SaveCommand}" Margin="0,10,0,0" Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>
