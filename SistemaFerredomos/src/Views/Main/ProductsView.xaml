<UserControl x:Class="SistemaFerredomos.src.Views.Main.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SistemaFerredomos.src.ViewModels.Main"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- BOTONES DE ACCIÓN -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="➕ Agregar Producto" Command="{Binding AddCommand}" Width="130"/>
            <Button Content="✏️ Editar Producto" Command="{Binding EditCommand}" Margin="10,0,0,0" Width="130"
                    IsEnabled="{Binding SelectedProduct, Converter={StaticResource NullToBoolConverter}}"/>
            <Button Content="🗑️ Eliminar Producto" Command="{Binding DeleteCommand}" Margin="10,0,0,0" Width="130"
                    IsEnabled="{Binding SelectedProduct, Converter={StaticResource NullToBoolConverter}}"/>
        </StackPanel>

        <!-- TABLA DE PRODUCTOS -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding stock}" Width="80"/>
                <DataGridTextColumn Header="Precio Compra" Binding="{Binding PurchasePrice}" Width="100"/>
                <DataGridTextColumn Header="Precio Venta" Binding="{Binding SalePrice}" Width="100"/>
                <DataGridTextColumn Header="Proveedor" Binding="{Binding Supplier.Name}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
